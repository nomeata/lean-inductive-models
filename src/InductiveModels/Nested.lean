import InductiveModels.Nested.Gen
import InductiveModels.Nested.Rules
import InductiveModels.Nested.Iso

/-!
# The nested construction

**Rung three of the tower**, built on the mutual construction below it and on
the shared core in `InductiveModels.Gen.*`. `InductiveModels.Model` is the
facade that keeps this reachable under its historical name.
-/
